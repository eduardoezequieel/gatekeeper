import { ShortNamesPipe } from './short-names.pipe';

describe('ShortNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
